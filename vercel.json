{
  "buildCommand": "cd breachmap/frontend && npm install && npm run build",
  "outputDirectory": "breachmap/frontend/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
